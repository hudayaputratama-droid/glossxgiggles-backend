from fastapi import APIRouter, HTTPException, status\nimport httpx\nfrom typing import Optional\nimport random\n\nrouter = APIRouter()\n\n# External APIs for jokes\nJOKE_APIS = [\n    {\n        \"name\": \"JokeAPI\",\n        \"url\": \"https://v2.jokeapi.dev/joke/Any?format=json\",\n        \"parser\": \"jokeapi\"\n    },\n    {\n        \"name\": \"Official Joke API\",\n        \"url\": \"https://official-joke-api.appspot.com/random_joke\",\n        \"parser\": \"official\"\n    },\n    {\n        \"name\": \"Random Useless Facts\",\n        \"url\": \"https://uselessfacts.jsfc.rocks/random?language=en\",\n        \"parser\": \"facts\"\n    }\n]\n\nclass JokeParser:\n    @staticmethod\n    def parse_jokeapi(data: dict) -> dict:\n        \"\"\"Parse JokeAPI response\"\"\"\n        if data.get('type') == 'single':\n            return {\n                \"joke\": data.get('joke'),\n                \"category\": data.get('category'),\n                \"source\": \"JokeAPI\",\n                \"type\": \"single\"\n            }\n        else:\n            return {\n                \"joke\": f\"{data.get('setup')} ... {data.get('delivery')}\",\n                \"category\": data.get('category'),\n                \"source\": \"JokeAPI\",\n                \"type\": \"two-part\"\n            }\n    \n    @staticmethod\n    def parse_official(data: dict) -> dict:\n        \"\"\"Parse Official Joke API response\"\"\"\n        return {\n            \"joke\": f\"{data.get('setup')} ... {data.get('punchline')}\",\n            \"category\": data.get('type'),\n            \"source\": \"Official Joke API\",\n            \"type\": \"two-part\",\n            \"id\": data.get('id')\n        }\n    \n    @staticmethod\n    def parse_facts(data: dict) -> dict:\n        \"\"\"Parse Random Useless Facts response\"\"\"\n        return {\n            \"joke\": data.get('text'),\n            \"category\": \"Facts\",\n            \"source\": \"Random Useless Facts\",\n            \"type\": \"fact\"\n        }\n\n@router.get(\"/random\")\nasync def get_random_joke():\n    \"\"\"\n    Get a random joke from multiple external APIs.\n    Randomly selects between different joke sources.\n    \"\"\"\n    async with httpx.AsyncClient(timeout=10.0) as client:\n        # Select random API\n        api = random.choice(JOKE_APIS)\n        \n        try:\n            response = await client.get(api[\"url\"])\n            response.raise_for_status()\n            data = response.json()\n            \n            # Parse based on source\n            if api[\"parser\"] == \"jokeapi\":\n                joke = JokeParser.parse_jokeapi(data)\n            elif api[\"parser\"] == \"official\":\n                joke = JokeParser.parse_official(data)\n            elif api[\"parser\"] == \"facts\":\n                joke = JokeParser.parse_facts(data)\n            \n            return {\n                \"success\": True,\n                \"data\": joke\n            }\n        \n        except httpx.HTTPError as e:\n            raise HTTPException(\n                status_code=status.HTTP_503_SERVICE_UNAVAILABLE,\n                detail=f\"Failed to fetch joke: {str(e)}\"\n            )\n\n@router.get(\"/random/programming\")\nasync def get_programming_joke():\n    \"\"\"\n    Get a random programming joke.\n    \"\"\"\n    async with httpx.AsyncClient(timeout=10.0) as client:\n        try:\n            response = await client.get(\n                \"https://v2.jokeapi.dev/joke/Programming?format=json\"\n            )\n            response.raise_for_status()\n            data = response.json()\n            \n            joke = JokeParser.parse_jokeapi(data)\n            \n            return {\n                \"success\": True,\n                \"data\": joke\n            }\n        \n        except httpx.HTTPError as e:\n            raise HTTPException(\n                status_code=status.HTTP_503_SERVICE_UNAVAILABLE,\n                detail=f\"Failed to fetch programming joke: {str(e)}\"\n            )\n\n@router.get(\"/random/knock-knock\")\nasync def get_knock_knock_joke():\n    \"\"\"\n    Get a random knock-knock joke.\n    \"\"\"\n    async with httpx.AsyncClient(timeout=10.0) as client:\n        try:\n            response = await client.get(\n                \"https://v2.jokeapi.dev/joke/Knock-Knock?format=json\"\n            )\n            response.raise_for_status()\n            data = response.json()\n            \n            joke = JokeParser.parse_jokeapi(data)\n            \n            return {\n                \"success\": True,\n                \"data\": joke\n            }\n        \n        except httpx.HTTPError as e:\n            raise HTTPException(\n                status_code=status.HTTP_503_SERVICE_UNAVAILABLE,\n                detail=f\"Failed to fetch knock-knock joke: {str(e)}\"\n            )\n\n@router.get(\"/by-category/{category}\")\nasync def get_joke_by_category(category: str):\n    \"\"\"\n    Get a joke by category.\n    Valid categories: Any, Miscellaneous, Programming, Knock-Knock\n    \"\"\"\n    valid_categories = [\"Any\", \"Miscellaneous\", \"Programming\", \"Knock-Knock\"]\n    \n    if category not in valid_categories:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST,\n            detail=f\"Invalid category. Valid categories: {', '.join(valid_categories)}\"\n        )\n    \n    async with httpx.AsyncClient(timeout=10.0) as client:\n        try:\n            response = await client.get(\n                f\"https://v2.jokeapi.dev/joke/{category}?format=json\"\n            )\n            response.raise_for_status()\n            data = response.json()\n            \n            joke = JokeParser.parse_jokeapi(data)\n            \n            return {\n                \"success\": True,\n                \"data\": joke\n            }\n        \n        except httpx.HTTPError as e:\n            raise HTTPException(\n                status_code=status.HTTP_503_SERVICE_UNAVAILABLE,\n                detail=f\"Failed to fetch joke: {str(e)}\"\n            )\n\n@router.get(\"/multiple/{count}\")\nasync def get_multiple_jokes(count: int = 5):\n    \"\"\"\n    Get multiple random jokes at once.\n    Default: 5 jokes\n    \"\"\"\n    if count < 1 or count > 20:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST,\n            detail=\"Count must be between 1 and 20\"\n        )\n    \n    jokes = []\n    async with httpx.AsyncClient(timeout=10.0) as client:\n        for _ in range(count):\n            api = random.choice(JOKE_APIS)\n            try:\n                response = await client.get(api[\"url\"])\n                response.raise_for_status()\n                data = response.json()\n                \n                if api[\"parser\"] == \"jokeapi\":\n                    joke = JokeParser.parse_jokeapi(data)\n                elif api[\"parser\"] == \"official\":\n                    joke = JokeParser.parse_official(data)\n                elif api[\"parser\"] == \"facts\":\n                    joke = JokeParser.parse_facts(data)\n                \n                jokes.append(joke)\n            except httpx.HTTPError:\n                continue\n    \n    return {\n        \"success\": True,\n        \"count\": len(jokes),\n        \"data\": jokes\n    }\n